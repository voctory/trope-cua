# Trope CUA Skill

Agent harness skill instructions for using the Windows Trope CUA driver.

Install with:

```powershell
.\scripts\install-skills-windows.ps1
```

The installer copies this directory into detected skill folders without overwriting existing user edits.
